# inikolaou.github.io

Personal site. Hand-written static HTML/CSS/JS, no build step. Deployed via GitHub Pages from `main`.

## Edit

- Content lives in `index.html`.
- Tokens (type, color, rhythm) are at the top of `styles.css` under `:root`.

## To do

- Drop a portrait at `assets/portrait.jpg` (4:5, ≥1600px long edge, B&W or duotone) and replace the placeholder block in the hero.
- Add a CV at `cv.pdf` and link it from the top bar if desired.
- Add an OG card at `assets/og.jpg` (1200×630).
